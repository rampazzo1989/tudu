import {PublicPopoverProps} from 'react-native-popover-view/dist/Popover';

export interface PopoverMenuProps extends PublicPopoverProps {
  statusBarTranslucent?: boolean;
  animationType?: 'none';
}
