import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  skeleton: {
    width: '100%',
    height: 60,
    borderRadius: 8,
    backgroundColor: '#3C414A',
    marginBottom: 4,
  },
});
